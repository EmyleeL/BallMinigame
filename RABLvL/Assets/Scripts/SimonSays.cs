using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SimonSays : MonoBehaviour
{
    // use buttons in here?

    // Start is called before the first frame update
    void Start()
    {
        // create simon says (red, red blue, red blue green, red blue green yellow, etc.)
        // randomize simon says pattern
    }

    // Update is called once per frame
    void Update()
    {
        // did press correct button? continue pattern
        // didn't press correct button? restart.
        // correct after 5? game win
        // game win? open door to "next level"
    }
}
